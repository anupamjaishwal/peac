/**
 * Created on 1/1/18.
 */
({
    formatSSN : function (cmp, event, helper) {
        helper.formatSSN(cmp, event, helper);
    },

    doInit : function (cmp, event, helper) {
        helper.formatSSN(cmp, event, helper);
    }

})