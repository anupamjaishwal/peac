({
	doInit : function(cmp, event, helper) {
		helper.grabOppInfoHelper (cmp, event, helper);
	},
    saveButtonClicked : function(cmp, event, helper) {
		helper.savePayOffsHelper (cmp, event, helper);
	}
})