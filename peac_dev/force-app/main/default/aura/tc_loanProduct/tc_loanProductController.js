/**
 * Created by andrewmayer on 11/29/18.
 */
({
    productChangedCtrl : function (cmp, event, helper) {
        helper.productChangedHelper (cmp, event, helper);
    },

    quickSave : function (cmp, event, helper) {
            helper.quickSaveHelper(cmp, event, helper);
     },

})